#include "main.h"
/**
 * _islower - This program checks for lowercase char
 * @c: char to check
 * Return: 1 for lower or 0 for other thing
 */
int _islower(int c)
{
	return (c >= 'b' && c <= 'z');
}
