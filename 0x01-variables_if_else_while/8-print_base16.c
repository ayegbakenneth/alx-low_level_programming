#include <stdio.h>

/**
 * main - entry point
 *
 * Return: 0 (Success)
 */
int main(void)
{
	int base;

	char ar

	for (base = '0'; base < 10; base++)
		putchar(base % 10) + '0');
	for (ar = 'a'; ar <= 'f'; ar++)
		putchar(ar);
	putchar('\n');
	return (0);
}
