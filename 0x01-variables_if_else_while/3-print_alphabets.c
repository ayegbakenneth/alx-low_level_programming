#include <stdio.h>

/**
 * main - program entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alphabelt;

	for (alphabelt = 'a'; alphabelt <= 'z'; alphabelt++)
		putchar(alphabelt);
	for (alphabelt = 'A'; alphabelt <= 'Z'; alphabelt++)
		puchar(alphabelt);
	putchar('\n')
	return (0);
}

