#include <stdio.h>
/**
 * main - The program name
 * @argc: For counting
 * @argv: The storing vector
 * Return: 0 always
 */
int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);
	return (0);
}
