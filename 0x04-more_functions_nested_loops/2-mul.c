#include "main.h"
#include <stdio.h>
/**
 * mul - multiplication of numbers
 * @a: the first number
 * @b: - the second number
 * Return: 0 always
 */
int mul(int a, int b)
{
int result = a * b;
return (result);
}
